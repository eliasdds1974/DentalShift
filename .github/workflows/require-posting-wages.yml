name: Require posting wages

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/require-posting-wages.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Finalize wage and repost behavior
        run: python .github/scripts/finalize_availability_wage.py
      - name: Commit source changes
        run: |
          git config user.name "DentalShift automation"
          git config user.email "actions@users.noreply.github.com"
          git add components/WorkflowWorkspaceV2.tsx components/OfficeWorkspaceV2.tsx
          if git diff --cached --quiet; then
            echo "No changes needed"
          else
            git commit -m "Finalize availability wage and repost behavior"
            git push
          fi
