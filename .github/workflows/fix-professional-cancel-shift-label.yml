name: Fix professional Cancel Shift label

on:
  push:
    paths:
      - '.github/scripts/fix-professional-cancel-shift-label.py'
      - '.github/workflows/fix-professional-cancel-shift-label.yml'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply professional Cancel Shift label fix
        run: python .github/scripts/fix-professional-cancel-shift-label.py
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Build
        run: npm run build
      - name: Commit professional Cancel Shift label fix
        run: |
          git config user.name "dentalshift-bot"
          git config user.email "bot@users.noreply.github.com"
          git add components/WorkflowWorkspaceV2.tsx
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Fix Professional Portal scheduled card Cancel Shift label"
            git push
          fi
