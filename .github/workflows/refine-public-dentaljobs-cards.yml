name: Refine public DentalJobs cards

on:
  push:
    branches: [main]
    paths:
      - '.github/scripts/refine-public-dentaljobs-cards.py'
      - '.github/workflows/refine-public-dentaljobs-cards.yml'

permissions:
  contents: write

jobs:
  patch-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply public DentalJobs card refinements
        run: python .github/scripts/refine-public-dentaljobs-cards.py
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Production build
        run: npm run build
      - name: Commit verified application change
        run: |
          git config user.name "dentalshift-bot"
          git config user.email "bot@users.noreply.github.com"
          git add app/classifieds/page.tsx
          if git diff --cached --quiet; then
            echo "No application change needed"
          else
            git commit -m "Refine public DentalJobs cards"
            git push
          fi
