name: Publish DentalJobs office ads

on:
  push:
    branches:
      - main
    paths:
      - '.github/scripts/publish-dentaljobs-office-ads.py'
      - '.github/workflows/publish-dentaljobs-office-ads.yml'

permissions:
  contents: write

jobs:
  patch-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply DentalJobs publishing connection
        run: python .github/scripts/publish-dentaljobs-office-ads.py
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Production build
        run: npm run build
      - name: Commit verified application change
        run: |
          git config user.name "dentalshift-bot"
          git config user.email "bot@users.noreply.github.com"
          git add app/classifieds/page.tsx
          if git diff --cached --quiet; then
            echo "No application change needed"
          else
            git commit -m "Publish office DentalJobs ads to Supabase"
            git push
          fi
