name: Polish professional available card
on:
  push:
    branches: [main]
    paths:
      - '.github/scripts/polish-professional-available-card.py'
      - '.github/workflows/polish-professional-available-card.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python .github/scripts/polish-professional-available-card.py
      - name: Commit source changes
        run: |
          git config user.name "DentalShift automation"
          git config user.email "actions@users.noreply.github.com"
          git add components/WorkflowWorkspaceV2.tsx
          if git diff --cached --quiet; then echo "No changes needed"; else git commit -m "Polish professional available card" && git push; fi
