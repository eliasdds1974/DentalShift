name: Add My DentalJobs management

on:
  push:
    branches: [main]
    paths:
      - '.github/scripts/add-my-dentaljobs-management.py'
      - '.github/workflows/add-my-dentaljobs-management.yml'

permissions:
  contents: write

jobs:
  patch-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply My DentalJobs management
        run: python .github/scripts/add-my-dentaljobs-management.py
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Production build
        run: npm run build
      - name: Commit verified application change
        run: |
          git config user.name "dentalshift-bot"
          git config user.email "bot@users.noreply.github.com"
          git add app/classifieds/page.tsx
          if git diff --cached --quiet; then
            echo "No application change needed"
          else
            git commit -m "Add My DentalJobs posting management"
            git push
          fi
