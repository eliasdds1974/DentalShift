name: Profession color DentalJobs cards

on:
  push:
    branches: [main]
    paths:
      - '.github/scripts/profession-color-job-cards.py'
      - '.github/workflows/profession-color-job-cards.yml'

permissions:
  contents: write

jobs:
  patch-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply profession color styling
        run: python .github/scripts/profession-color-job-cards.py
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Production build
        run: npm run build
      - name: Commit verified application change
        run: |
          git config user.name "dentalshift-bot"
          git config user.email "bot@users.noreply.github.com"
          git add app/classifieds/page.tsx
          if git diff --cached --quiet; then
            echo "No application change needed"
          else
            git commit -m "Color professional DentalJobs cards by profession"
            git push
          fi
