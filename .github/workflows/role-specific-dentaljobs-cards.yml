name: Role-specific DentalJobs cards

on:
  push:
    branches:
      - main
    paths:
      - '.github/scripts/role-specific-dentaljobs-cards.py'
      - '.github/workflows/role-specific-dentaljobs-cards.yml'

permissions:
  contents: write

jobs:
  patch-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply role-specific DentalJobs cards
        run: python .github/scripts/role-specific-dentaljobs-cards.py
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Production build
        run: npm run build
      - name: Commit verified application change
        run: |
          git config user.name "dentalshift-bot"
          git config user.email "bot@users.noreply.github.com"
          git add app/classifieds/page.tsx
          if git diff --cached --quiet; then
            echo "No application change needed"
          else
            git commit -m "Show DentalJobs action by portal role"
            git push
          fi
