name: Finalize office posting card layout

on:
  push:
    paths:
      - '.github/workflows/finalize-office-posting-card-layout.yml'
      - '.github/scripts/finalize-office-posting-card-layout.py'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Apply final office posting card layout
        run: python .github/scripts/finalize-office-posting-card-layout.py
      - name: Install dependencies
        run: npm install
      - name: Build
        run: npm run build
      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/classifieds/page.tsx
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Finalize office posting card layout"
          git push origin main
