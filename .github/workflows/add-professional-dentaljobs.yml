name: Add professional DentalJobs

on:
  push:
    branches: [main]
    paths:
      - '.github/scripts/add-professional-dentaljobs.py'
      - '.github/workflows/add-professional-dentaljobs.yml'

permissions:
  contents: write

jobs:
  patch-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply professional DentalJobs implementation
        run: python .github/scripts/add-professional-dentaljobs.py
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Production build
        run: npm run build
      - name: Commit verified application change
        run: |
          git config user.name "dentalshift-bot"
          git config user.email "bot@users.noreply.github.com"
          git add app/classifieds/page.tsx
          if git diff --cached --quiet; then
            echo "No application change needed"
          else
            git commit -m "Build professional DentalJobs publishing and management"
            git push
          fi
