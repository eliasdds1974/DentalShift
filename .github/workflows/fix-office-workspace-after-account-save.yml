name: Fix office workspace after account save

on:
  push:
    branches: [main]
    paths:
      - '.github/scripts/fix_office_workspace_after_account_save.py'
      - '.github/workflows/fix-office-workspace-after-account-save.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fix office workspace refresh
        run: python .github/scripts/fix_office_workspace_after_account_save.py
      - name: Commit source changes
        run: |
          git config user.name "DentalShift automation"
          git config user.email "actions@users.noreply.github.com"
          git add app/page.tsx
          if git diff --cached --quiet; then
            echo "No changes needed"
          else
            git commit -m "Fix office workspace after account save"
            git push
          fi
